{
  "name": "limitbeacon-streamdeck",
  "version": "1.0.0",
  "description": "AI usage monitor for Codex on Stream Deck",
  "author": "Milan Jelicanin",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test",
    "validate": "npm run build && streamdeck validate com.jelicanin.limitbeacon.sdPlugin",
    "check": "npm run typecheck && npm test && node scripts/check-generated.mjs && streamdeck validate com.jelicanin.limitbeacon.sdPlugin"
  },
  "dependencies": {
    "@elgato/streamdeck": "^2.1.0"
  },
  "devDependencies": {
    "@elgato/cli": "^1.7.4",
    "@types/node": "^24.13.3",
    "esbuild": "^0.28.1",
    "tsx": "^4.23.1",
    "typescript": "^7.0.2"
  }
}
